# Задание 3: Сохраняем данные в JSON файл
# Максимально просто для новичка

print("=" * 50)
print("ЗАДАНИЕ 3: Сохраняем новости в файл")
print("=" * 50)

# Создаём данные (как в задании 2)
новости = [
    {"номер": 1, "заголовок": "Hacker News is great", "комментарии": 234},
    {"номер": 2, "заголовок": "Python 3.12 Released", "комментарии": 15},
    {"номер": 3, "заголовок": "Understanding AI Models", "комментарии": 4},
]

print("\n📰 Наши новости:")
for новость in новости:
    print(f"{новость['номер']}. {новость['заголовок']} - {новость['комментарии']} коммент.")

print("\n💾 Сохраняем в файл data.json...")

# Пишем данные в файл (очень просто)
try:
    # Открываем файл для записи
    файл = open("data.json", "w", encoding="utf-8")
    
    # Начинаем запись
    файл.write("[\n")
    
    # Записываем каждую новость
    for i, новость in enumerate(новости):
        файл.write("  {\n")
        файл.write(f'    "номер": {новость["номер"]},\n')
        файл.write(f'    "заголовок": "{новость["заголовок"]}",\n')
        файл.write(f'    "комментарии": {новость["комментарии"]}\n')
        файл.write("  }")
        
        # Добавляем запятую, если не последняя новость
        if i < len(новости) - 1:
            файл.write(",")
        файл.write("\n")
    
    файл.write("]")
    файл.close()
    
    print("✅ Файл data.json успешно создан!")
    
except Exception as e:
    print(f"❌ Ошибка: {e}")

# Показываем что в файле
print("\n📄 Содержимое файла data.json:")
print("Откройте файл data.json в любом текстовом редакторе")
print("или посмотрите его в папке с программой.")